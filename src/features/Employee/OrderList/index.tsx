import { Order } from "./type";

export const OrderList = ({ orders }: { orders: Order[] }) => {
  console.log(orders);

  return (
    <>
      <h1>OrderList</h1>
      <div>TODO</div>
    </>
  );
};
